{
  "name": "replynodes-mcp",
  "displayName": "Replynodes",
  "version": "0.1.2",
  "description": "Connect Claude to your Replynodes account over MCP — list social channels, schedule posts, and generate images/video for posts.",
  "author": {
    "name": "Replynodes",
    "url": "https://replynodes.com"
  },
  "homepage": "https://replynodes.com",
  "repository": "https://github.com/replynodes/replynodes-mcp",
  "license": "MIT",
  "keywords": ["mcp", "replynodes", "social-media", "ai-agent"],
  "mcpServers": {
    "replynodes": {
      "command": "npx",
      "args": ["-y", "@replynodes/mcp"],
      "env": {
        "REPLYNODES_API_KEY": "${user_config.api_key}"
      }
    }
  },
  "userConfig": {
    "api_key": {
      "type": "string",
      "title": "Replynodes API Key",
      "description": "Create one at app.replynodes.com → Developers → API Keys & Agents",
      "sensitive": true,
      "required": true
    }
  }
}
