{
  "name": "replynodes",
  "version": "0.1.4",
  "description": "Read-only web search, scraping, crawling, Reddit, YouTube, App Store, Hacker News, and brand-intelligence MCP server for Cursor. Connect to the canonical public endpoint, authenticate with your claimed API key, and call tools via Cursor's Tools panel.",
  "readme": "README.md",
  "icon": "assets/logo.svg",
  "category": "data-and-analytics",
  "tags": [
    "mcp",
    "web-search",
    "scraping",
    "crawl",
    "reddit",
    "youtube",
    "app-store",
    "hacker-news",
    "brand-intelligence",
    "read-only"
  ],
  "repository": "https://github.com/replynodes/replynodes-mcp",
  "homepage": "https://replynodes.com",
  "variables": {
    "type": "object",
    "properties": {
      "REPLYNODES_API_KEY": {
        "type": "string",
        "title": "ReplyNodes API key",
        "description": "API key obtained from the ReplyNodes auth claim flow at https://replynodes.com/auth.md."
      }
    },
    "required": ["REPLYNODES_API_KEY"]
  },
  "mcpServers": {
    "replynodes": {
      "url": "https://mcp.replynodes.com/mcp",
      "headers": {
        "Authorization": "Bearer ${REPLYNODES_API_KEY}"
      }
    }
  }
}
